
test:
  override:
    - sbt scalaVersion
    - sbt test
  post:
    - sbt assembly
    - sbt "show assembly::assemblyJarName"
    - sbt "show assembly::assemblyOutputPath"
    - java -jar target/scala-2.10/scala-on-circle-assembly-0.1-SNAPSHOT.jar
