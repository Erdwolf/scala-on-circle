
test:
  override:
    - sbt --version
    - sbt --sbt-version
    - sbt test
  post:
     - sbt package
     - sbt --error "export packageBin::artifactPath"
     - java -jar `sbt --error "export packageBin::artifactPath"`
