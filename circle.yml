dependencies:
  override:
    - sbt update

test:
  override:
    - sbt test
  post:
    - sbt assembly
    - cp `sbt "export assembly::assemblyOutputPath"|tail -n1` $CIRCLE_ARTIFACTS/
    - java -jar `sbt "export assembly::assemblyOutputPath"|tail -n1`
